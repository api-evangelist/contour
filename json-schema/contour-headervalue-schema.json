{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/HeaderValue",
  "title": "HeaderValue",
  "type": "object",
  "description": "A header name-value pair.",
  "required": [
    "name",
    "value"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "HTTP header name."
    },
    "value": {
      "type": "string",
      "description": "HTTP header value."
    }
  }
}